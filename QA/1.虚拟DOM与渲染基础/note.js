// JSX如何编译为React元素？分析ReactElement.js中createElement的转换逻辑

// React元素的$$typeof字段为何使用Symbol？如何利用Symbol.for('react.element')防范XSS攻击？

// 首次渲染时ReactDOM.render()如何将虚拟DOM转为真实DOM？追踪createInstance调用链

// 分析ReactDOMComponent.js中appendInitialChild()如何构建DOM树层级结构

// 虚拟DOM树的递归创建过程在源码中的具体实现路径

// key和ref在createElement()中的特殊处理逻辑，为何它们不能通过props访问？

// 空元素（React.Fragment）在编译阶段的特殊处理机制

// 开发模式下Object.freeze()在元素创建中的作用

// __DEV__环境下的元素类型验证流程（isValidElementType实现）

// React元素与Fiber节点的核心区别是什么？

// 虚拟DOM如何优化跨平台渲染的抽象成本？

// 如何手动创建React元素并绕过JSX编译？
