// 提交三阶段：beforeMutation/mutation/layout的职责边界

// DOM批处理：分析commitPlacement节点插入算法

// 副作用收集：flags到DOM操作（commitDeletion）的转换

// useLayoutEffect在commitLayoutEffects中的同步执行逻辑

// 生命周期钩子（componentDidMount等）的调用位置

// 提交阶段错误边界处理机制

// commitWork的宿主环境操作分发逻辑

// 属性差异合并算法（style/className）

// 文本内容的高效更新策略

// Portal节点的跨容器挂载机制

// getSnapshotBeforeUpdate的提交前快照捕获

// 提交阶段的可中断性设计限制

// 副作用链表的遍历顺序优化

// 提交后ref的清理与更新

// 水合（hydration）失败的DOM修复策略