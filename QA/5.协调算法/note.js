// 三阶段Diff算法：剖析reconcileChildrenArray遍历策略

// Key映射机制：mapRemainingChildren的哈希表实现

// 节点移动检测：lastPlacedIndex优化原理

// 文本节点与空节点的碎片化处理

// 单节点到多节点的转换算法

// 多节点到单节点的销毁优化

// 嵌套数组（Fragment数组）的拍平策略

// 无Key长列表更新的性能黑洞分析

// 相同位置不同元素类型的销毁/重建判断

// 宿主组件与复合组件Diffing的递归差异

// 删除元素在协调阶段的标记过程

// 新增元素的位置寻优算法

// 动态列表排序变更的最优复用策略

// updateSlot中的Fiber复用条件矩阵

// 副作用标记（Placement/Deletion）的批量标记机制

// Diffing算法的O(n)复杂度保证原理